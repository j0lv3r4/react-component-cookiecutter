import { Outlet } from "react-router-dom";

const {{ cookiecutter.project_name_plural }}Page = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export { {{ cookiecutter.project_name_plural }}Page };
